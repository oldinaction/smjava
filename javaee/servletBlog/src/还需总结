问题：servletblog.sql文件在navicat中运行提示"ERROR 1005: Can't create table (errno: 121)"说是外键重复

1、JS中的setInterval(function(){},millisec); 多少毫秒执行一次

2、WEB-INF下的文件不能直接在浏览器的地址栏或者href=""访问，要通过servlet中的
WEB-INF目录下的jsp,html等文件中的js,css文件不能通过相对路径(../来获取上级目录)引入，而要使用绝对路径:/项目名/js等文件的目录/.js文件

3、有时候修改项目重新部署之后任然得到了上次的调试结果(感觉修改的部分没有起到作用)，解决方法：
	(1)查看修改的内容是否正确
	(2)查看Tomcat的webapp目录下对应的项目的文件是否重新部署成功
	(3)清除浏览器的历史记录
	
4、shift+alt+r 修改.java的文件名或者java文件里面的变量名，则其他地方也会改变
修改.html .jsp文件名，则还要手动修改其他用到此文件的地方

5、关于异常
异常名后面的提示比较重要
提示的异常，靠上方的提示信息是主要原因错误原因
控制台输出的提示，从下到上是表示程序的运行过程，下面的异常是因为上面的异常引起的
eg:
org.apache.jasper.JasperException 可能是jsp页面语法错误，如获取某些信息在语法上不符合

6、重定向不能重定向到WEB-INF下的页面












