<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:context="http://www.springframework.org/schema/context"
    xmlns:mvc="http://www.springframework.org/schema/mvc"
    xsi:schemaLocation="
        http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/mvc
        http://www.springframework.org/schema/mvc/spring-mvc.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd">

    <context:component-scan base-package="cn.aezo"/>
    
    <!-- 视图解析器:在方法返回的字符串中不含forward和redirect的加上前后缀 -->
	<bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<property name="prefix" value="/WEB-INF/jsp/"/><!-- 加上前缀 -->
		<property name="suffix" value=".jsp"/><!-- 加上后缀 -->
		<!-- 如果使用jstl的话，配置下面的属性 -->
    	<property name="viewClass" value="org.springframework.web.servlet.view.JstlView" />    
    </bean>
    
    <!-- 处理文件上传 -->
	<bean id="multipartResolver"  
	    class="org.springframework.web.multipart.commons.CommonsMultipartResolver" >  
	    <property name="defaultEncoding" value="utf-8"/> <!-- 默认编码 (ISO-8859-1) -->  
	    <property name="maxInMemorySize" value="10240"/> <!-- 最大内存大小 (10240)-->  
	    <property name="uploadTempDir" value="/upload/"/> <!-- 上传后的目录名 (WebUtils#TEMP_DIR_CONTEXT_ATTRIBUTE) -->  
	    <property name="maxUploadSize" value="-1"/> <!-- 最大文件大小，-1为无限止(-1) -->  
	</bean>
	
	<!-- mvc的annotation驱动。可以解析annotation里的参数，如produces="text/plain;charset=utf-8" -->
	<mvc:annotation-driven></mvc:annotation-driven>
	
	<!-- springmvc的拦截器 -->
	<mvc:interceptors>
		<!-- 拦截所有springmvc的url！ -->
		<bean class="cn.aezo.others.MyInterceptor"></bean>
		<!-- 拦截某类url或者不拦截某类url -->
		<mvc:interceptor>
			<mvc:mapping path="/param/user.do" /><!-- 拦截/param/user.do的url -->
			<bean class="cn.aezo.others.MyInterceptor2"></bean>
			<!-- <mvc:exclude-mapping path="/user/*"/> --><!-- 不拦截/user/路径下的所有url -->
		</mvc:interceptor>
	</mvc:interceptors>
</beans>